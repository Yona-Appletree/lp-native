import type { Meta, StoryObj } from '@storybook/react';
import { App } from './app';

const meta: Meta = {
  component: () => <div>Placeholder for app, no actual story for it.</div>,
  title: 'App',
};
export default meta;
type Story = StoryObj;

export const Primary = {
  args: {},
};
