import { z } from 'zod';
import { Vector1d } from '../../core/data/vector-1d';
import { zUint32Array } from '../../core/data/z-int32-array';

export const Layout1dKind = {
  key: 'layout-1d',
  description: 'Arranges several fixtures in a 1D space.',
  outputs: [],
  messages: [],
};
